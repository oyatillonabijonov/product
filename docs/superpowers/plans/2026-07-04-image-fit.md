# Tovar rasmi — borderless fit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tovar rasmlari kartochka/sahifada kadrni to'liq to'ldirsin (`object-cover`, oq/bo'sh joysiz), va yuklashda pro rasmlardagi baked bo'sh chekka avtomatik trim qilinsin.

**Architecture:** Ikki qism. (1) Ko'rsatish — barcha rasm zonasi `aspect-[4/5]` + `object-cover`. (2) Yuklash — admin brauzerida `normalizeImage(file)` faylni canvas'da bir xil rangли chekkasidan trim qiladi + `.webp` ga kichraytiradi, so'ng mavjud `uploadImage` R2'ga yuklaydi. Server o'zgarmaydi.

**Tech Stack:** React Router v7 SSR (Cloudflare Workers), TypeScript strict, Tailwind v4, vitest (node env), browser Canvas / `createImageBitmap` / `canvas.toBlob`.

## Global Constraints

- **Strict TypeScript, `any` taqiqlanadi.** Ambient React shim (no `@types/react`); `key` oladigan komponentlar `FC<{...}>` uslubida.
- **bun** ishlatiladi (`bun run lint`, `bun run test`), npm emas.
- **Bepul Cloudflare tarif:** rasm ishlashi faqat admin brauzerida — Worker CPU/bundle o'zgarmaydi.
- **Hex rang komponentlarda taqiq** — mavjud `--color-*` utility'lar. (Bu rejada rang qo'shilmaydi.)
- **Tailwind v4:** `shadow-[--var]` ishlatilmaydi; mavjud `shadow-apple` utility'lar.
- Test env `node` (DOM yo'q) — `vitest.config.ts` `include: src/**/*.test.ts`. Sof funksiyalargina unit-test qilinadi.
- **Commit qilma** foydalanuvchi tasdig'isiz (global qoida). Har task oxiridagi commit qadamini bajarishdan oldin tasdiq so'ra.

---

### Task 1: `contentBounds` — bir xil rangли chekkani aniqlovchi sof funksiya

**Files:**
- Create: `src/admin/lib/image-normalize.ts`
- Test: `src/admin/lib/image-normalize.test.ts`

**Interfaces:**
- Produces:
  - `interface PixelData { data: Uint8ClampedArray; width: number; height: number }`
  - `interface Bounds { x: number; y: number; width: number; height: number }`
  - `interface TrimOptions { tolerance?: number; maxTrimRatio?: number }`
  - `function contentBounds(img: PixelData, opts?: TrimOptions): Bounds`
  - Standartlar: `tolerance = 12` (0–255 har kanal), `maxTrimRatio = 0.42` (har tomondan maksimal kesish ulushi).

- [ ] **Step 1: Write the failing test**

`src/admin/lib/image-normalize.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { contentBounds, type PixelData } from './image-normalize';

// N×N RGBA grid quruvchi yordamchi: fill(x,y) → [r,g,b,a]
function grid(w: number, h: number, fill: (x: number, y: number) => [number, number, number, number]): PixelData {
  const data = new Uint8ClampedArray(w * h * 4);
  for (let y = 0; y < h; y++) {
    for (let x = 0; x < w; x++) {
      const i = (y * w + x) * 4;
      const [r, g, b, a] = fill(x, y);
      data[i] = r; data[i + 1] = g; data[i + 2] = b; data[i + 3] = a;
    }
  }
  return { data, width: w, height: h };
}

const WHITE: [number, number, number, number] = [255, 255, 255, 255];
const RED: [number, number, number, number] = [220, 40, 40, 255];

describe('contentBounds', () => {
  it('oq chekkani kesib, markazdagi kontent chegarasini qaytaradi', () => {
    // 4×4: tashqi halqa oq, markaziy 2×2 qizil
    const img = grid(4, 4, (x, y) => (x >= 1 && x <= 2 && y >= 1 && y <= 2 ? RED : WHITE));
    expect(contentBounds(img)).toEqual({ x: 1, y: 1, width: 2, height: 2 });
  });

  it('bir xil rangли (butunlay oq) rasm → no-op, 0×0 qaytarmaydi', () => {
    const img = grid(4, 4, () => WHITE);
    expect(contentBounds(img)).toEqual({ x: 0, y: 0, width: 4, height: 4 });
  });

  it('maxTrimRatio dan oshadigan bo\'sh joyni kesmaydi (himoya)', () => {
    // 10×10: faqat (5,5) bitta qizil piksel; qolgani oq.
    // Standart maxTrimRatio 0.42 → har tomondan max floor(10*0.42)=4 px kesiladi.
    // Demak top/left ≥ 4 ga clamp, bottom/right ≤ 5 ga clamp qilinadi.
    const img = grid(10, 10, (x, y) => (x === 5 && y === 5 ? RED : WHITE));
    expect(contentBounds(img)).toEqual({ x: 4, y: 4, width: 2, height: 2 });
  });

  it('shaffof chekkani (alpha) kesadi', () => {
    // 4×4: tashqi halqa to'liq shaffof, markaz 2×2 qizil (alpha 255)
    const img = grid(4, 4, (x, y) => (x >= 1 && x <= 2 && y >= 1 && y <= 2 ? RED : [0, 0, 0, 0]));
    expect(contentBounds(img)).toEqual({ x: 1, y: 1, width: 2, height: 2 });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bunx vitest run src/admin/lib/image-normalize.test.ts`
Expected: FAIL — `contentBounds` / module not found.

- [ ] **Step 3: Write minimal implementation**

`src/admin/lib/image-normalize.ts`:

```ts
export interface PixelData {
  data: Uint8ClampedArray;
  width: number;
  height: number;
}

export interface Bounds {
  x: number;
  y: number;
  width: number;
  height: number;
}

export interface TrimOptions {
  tolerance?: number;
  maxTrimRatio?: number;
}

function corner(img: PixelData, x: number, y: number): [number, number, number] {
  const i = (y * img.width + x) * 4;
  return [img.data[i], img.data[i + 1], img.data[i + 2]];
}

/**
 * Rasmning to'rt chekkasidan ichkariga skanerlab, bir xil rangли (yoki shaffof)
 * chekkani tashlab, mahsulot kontenti chegarasini qaytaradi. Amator suratда
 * (bir xil chekka yo'q) butun rasm qaytadi. maxTrimRatio har tomondan kesishni
 * cheklaydi — bir rangга yaqin rasmni buzmaslik uchun.
 */
export function contentBounds(img: PixelData, opts: TrimOptions = {}): Bounds {
  const tol = opts.tolerance ?? 12;
  const maxRatio = opts.maxTrimRatio ?? 0.42;
  const { data, width: w, height: h } = img;

  // Chekka-namuna rangi: to'rt burchak o'rtachasi.
  const c0 = corner(img, 0, 0);
  const c1 = corner(img, w - 1, 0);
  const c2 = corner(img, 0, h - 1);
  const c3 = corner(img, w - 1, h - 1);
  const refR = (c0[0] + c1[0] + c2[0] + c3[0]) / 4;
  const refG = (c0[1] + c1[1] + c2[1] + c3[1]) / 4;
  const refB = (c0[2] + c1[2] + c2[2] + c3[2]) / 4;

  const isBg = (x: number, y: number): boolean => {
    const i = (y * w + x) * 4;
    if (data[i + 3] < 16) return true; // shaffof
    return (
      Math.abs(data[i] - refR) <= tol &&
      Math.abs(data[i + 1] - refG) <= tol &&
      Math.abs(data[i + 2] - refB) <= tol
    );
  };

  const rowHasContent = (y: number): boolean => {
    for (let x = 0; x < w; x++) if (!isBg(x, y)) return true;
    return false;
  };
  const colHasContent = (x: number): boolean => {
    for (let y = 0; y < h; y++) if (!isBg(x, y)) return true;
    return false;
  };

  let top = 0;
  while (top < h && !rowHasContent(top)) top++;
  if (top === h) return { x: 0, y: 0, width: w, height: h }; // butunlay bg → no-op

  let bottom = h - 1;
  while (bottom > top && !rowHasContent(bottom)) bottom--;
  let left = 0;
  while (left < w && !colHasContent(left)) left++;
  let right = w - 1;
  while (right > left && !colHasContent(right)) right--;

  // maxTrimRatio clamp — har tomondan ortiqcha kesishni cheklash.
  const maxX = Math.floor(w * maxRatio);
  const maxY = Math.floor(h * maxRatio);
  top = Math.min(top, maxY);
  left = Math.min(left, maxX);
  bottom = Math.max(bottom, h - 1 - maxY);
  right = Math.max(right, w - 1 - maxX);

  return { x: left, y: top, width: right - left + 1, height: bottom - top + 1 };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bunx vitest run src/admin/lib/image-normalize.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Lint**

Run: `bun run lint`
Expected: no errors.

- [ ] **Step 6: Commit** (foydalanuvchi tasdig'idan keyin)

```bash
git add src/admin/lib/image-normalize.ts src/admin/lib/image-normalize.test.ts
git commit -m "feat: contentBounds — rasm chekkasini trim aniqlovchi sof funksiya"
```

---

### Task 2: `normalizeImage` glue + yuklash chaqiruvlariga ulash

**Files:**
- Modify: `src/admin/lib/image-normalize.ts` (append)
- Modify: `src/admin/ProductForm.tsx:107-121`
- Modify: `src/admin/VariantEditor.tsx:66-75`
- Modify: `src/admin/BrandForm.tsx:18-27`

**Interfaces:**
- Consumes: `contentBounds` (Task 1).
- Produces: `function normalizeImage(file: File, opts?: NormalizeOptions): Promise<File>`
  - `interface NormalizeOptions { maxSize?: number; quality?: number; tolerance?: number; maxTrimRatio?: number }`
  - Standartlar: `maxSize = 1600`, `quality = 0.85`. Xato/qo'llab-quvvatlanmasa — original `file` qaytadi (graceful).

- [ ] **Step 1: `normalizeImage` ni qo'shish**

`src/admin/lib/image-normalize.ts` oxiriga:

```ts
export interface NormalizeOptions {
  maxSize?: number;
  quality?: number;
  tolerance?: number;
  maxTrimRatio?: number;
}

/**
 * Faylni brauzerда: bir xil rangли chekkasidan trim qiladi, uzun tomonni maxSize
 * gacha kichraytiradi, .webp qilib qaytaradi. Hech qanday fon/kanvas qo'shmaydi.
 * Har qanday xatoda originalни o'zgartirmasdan qaytaradi (yuklash to'xtamasligi uchun).
 */
export async function normalizeImage(file: File, opts: NormalizeOptions = {}): Promise<File> {
  const maxSize = opts.maxSize ?? 1600;
  const quality = opts.quality ?? 0.85;
  try {
    const bitmap = await createImageBitmap(file);
    const src = document.createElement('canvas');
    src.width = bitmap.width;
    src.height = bitmap.height;
    const sctx = src.getContext('2d');
    if (!sctx) return file;
    sctx.drawImage(bitmap, 0, 0);
    bitmap.close();

    const imageData = sctx.getImageData(0, 0, src.width, src.height);
    const b = contentBounds(
      { data: imageData.data, width: src.width, height: src.height },
      { tolerance: opts.tolerance, maxTrimRatio: opts.maxTrimRatio },
    );

    const scale = Math.min(1, maxSize / Math.max(b.width, b.height));
    const outW = Math.max(1, Math.round(b.width * scale));
    const outH = Math.max(1, Math.round(b.height * scale));

    const out = document.createElement('canvas');
    out.width = outW;
    out.height = outH;
    const octx = out.getContext('2d');
    if (!octx) return file;
    octx.drawImage(src, b.x, b.y, b.width, b.height, 0, 0, outW, outH);

    const blob = await new Promise<Blob | null>((resolve) =>
      out.toBlob((r) => resolve(r), 'image/webp', quality),
    );
    if (!blob) return file;

    const name = file.name.replace(/\.[^.]+$/, '') + '.webp';
    return new File([blob], name, { type: 'image/webp' });
  } catch {
    return file;
  }
}
```

- [ ] **Step 2: `ProductForm` yuklashlariga ulash**

`src/admin/ProductForm.tsx` — importга `normalizeImage` qo'shish. Fayl boshidagi import (`./api` importi yonida):

```ts
import { normalizeImage } from './lib/image-normalize';
```

`uploadMain` va `uploadGallery` (107–121-qatorlar) ni yangilash:

```ts
  async function uploadMain(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (!file) return;
    setBusy(true);
    try { const { imageUrl } = await uploadImage(await normalizeImage(file)); set('imageUrl', imageUrl); }
    catch { setError('Rasm yuklanmadi'); } finally { setBusy(false); }
  }

  async function uploadGallery(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (!file) return;
    setBusy(true);
    try { const { imageUrl } = await uploadImage(await normalizeImage(file)); set('images', [...form.images, imageUrl]); }
    catch { setError('Rasm yuklanmadi'); } finally { setBusy(false); }
  }
```

- [ ] **Step 3: `VariantEditor` yuklashiga ulash**

`src/admin/VariantEditor.tsx` — importга qo'shish:

```ts
import { normalizeImage } from './lib/image-normalize';
```

`uploadVariantImage` (66–75-qatorlar) ичidagi `uploadImage(file)` ni almashtirish:

```ts
      const { imageUrl } = await uploadImage(await normalizeImage(file));
```

- [ ] **Step 4: `BrandForm` yuklashiga ulash**

`src/admin/BrandForm.tsx` — importга qo'shish:

```ts
import { normalizeImage } from './lib/image-normalize';
```

`onFile` (18–27-qatorlar) ичidagi `uploadImage(file)` ni almashtirish:

```ts
      const { imageUrl } = await uploadImage(await normalizeImage(file));
```

- [ ] **Step 5: Lint**

Run: `bun run lint`
Expected: no errors. (Agar `document` topilmasa — `src/admin/react-events.d.ts` yonidagi ambient tiplar brauzer global'larini beradi; xato bo'lsa `lib: ["DOM"]` root `tsconfig`da borligini tekshiring — admin allaqachon DOM'ga tayanadi, shuning uchun kutilmaydi.)

- [ ] **Step 6: Manual verify**

Run: `bun run dev` → `/admin` → mahsulot tahrirla → oq fonли rasm yukla → yuklangan preview'da bo'sh chekka kesilganini tekshir. Amator (real fonли) rasm yukla → deyarli o'zgarmaganini tekshir.

- [ ] **Step 7: Commit** (foydalanuvchi tasdig'idan keyin)

```bash
git add src/admin/lib/image-normalize.ts src/admin/ProductForm.tsx src/admin/VariantEditor.tsx src/admin/BrandForm.tsx
git commit -m "feat: yuklashda rasmni brauzerда trim + webp (normalizeImage)"
```

---

### Task 3: Display — `aspect-[4/5]` + `object-cover`

**Files:**
- Modify: `src/store/ProductCard.tsx:23,42`
- Modify: `src/store/Gallery.tsx:9-10,15-16`
- Modify: `src/store/CartPage.tsx:60`

**Interfaces:** yo'q (faqat JSX/Tailwind sinflari).

- [ ] **Step 1: `ProductCard` rasm zonasini portret cover qilish**

`src/store/ProductCard.tsx` — rasm konteyneri (23-qator):

```tsx
        className="aspect-[4/5] w-full flex items-center justify-center relative overflow-hidden bg-gradient-to-b from-row-alt to-segment"
```

Rasm (42-qator):

```tsx
            className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-[1.06]"
```

- [ ] **Step 2: `Gallery` ni portret cover qilish**

`src/store/Gallery.tsx` — asosiy rasm konteyneri (9-qator) va rasm (10-qator):

```tsx
      <div className="aspect-[4/5] bg-bg rounded-[22px] flex items-center justify-center overflow-hidden">
        {main ? <img src={main} alt={name} className="w-full h-full object-cover" /> : <span className="text-muted-2">{name}</span>}
      </div>
```

Thumbnail rasmi (16-qator) ni cover qilish:

```tsx
              <img src={img} alt="" className="w-full h-full object-cover rounded-lg" />
```

- [ ] **Step 3: `CartPage` thumbnail'ini cover qilish**

`src/store/CartPage.tsx` (59–60-qatorlar) — konteyner paddingини olib, rasmни cover qilish:

```tsx
              <div className="w-16 h-16 rounded-xl bg-bg flex items-center justify-center overflow-hidden shrink-0">
                {it.image ? <img src={it.image} alt="" className="w-full h-full object-cover" /> : null}
              </div>
```

- [ ] **Step 4: Lint**

Run: `bun run lint`
Expected: no errors.

- [ ] **Step 5: Manual verify**

Run: `bun run dev` → bosh sahifa/katalog: kartochka rasmlari portret kadrni to'ldiradi, oq/bo'sh joy yo'q. Mahsulot sahifasi (Image #2 muammosi): pastki bo'sh joy yo'q. Savat thumbnaillari to'ldiradi.

- [ ] **Step 6: Commit** (foydalanuvchi tasdig'idan keyin)

```bash
git add src/store/ProductCard.tsx src/store/Gallery.tsx src/store/CartPage.tsx
git commit -m "fix: tovar rasmi aspect-[4/5] + object-cover (borderless fit)"
```

---

## Self-Review

**Spec coverage:**
- Display (ProductCard/Gallery/CartPage `aspect-[4/5]` + cover) → Task 3. ✅
- `contentBounds` sof + unit-test → Task 1. ✅
- `normalizeImage` glue (trim + downscale + webp, fon qo'shmaydi, graceful fallback) → Task 2 Step 1. ✅
- Chaqiruv nuqtalari (ProductForm/VariantEditor/BrandForm) → Task 2 Steps 2–4. ✅
- Server o'zgarmaydi → hech bir task `api.admin.upload.tsx` ni tegmaydi. ✅
- Faqat yangi yuklashlar, migratsiya yo'q → planда migration fayl yo'q. ✅
- Testlar (4 holat) → Task 1 Step 1. ✅

**Placeholder scan:** har kod qadamида to'liq kod bor; "TODO"/"appropriate handling" yo'q. ✅

**Type consistency:** `PixelData`/`Bounds`/`TrimOptions`/`NormalizeOptions` Task 1–2 da bir xil ishlatiladi; `contentBounds(PixelData, TrimOptions)` va `normalizeImage(File, NormalizeOptions)` signaturalari mos. ✅
